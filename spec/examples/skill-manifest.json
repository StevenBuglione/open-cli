{
  "oasCliSkill": "1.0.0",
  "serviceId": "tickets",
  "summary": "Guidance for using the Tickets API through Open CLI",
  "toolGuidance": {
    "tickets:listTickets": {
      "whenToUse": ["Need to enumerate recent tickets"],
      "avoidWhen": ["You already know the ticket ID"],
      "examples": [
        {
          "goal": "List open tickets",
          "command": "ocli tickets tickets list --state open --format json"
        }
      ]
    }
  }
}
